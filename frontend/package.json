{
  "scripts": {
    "start": "serve -l 8000 .",
    "lint": "eslint . --format unix",
    "format": "prettier --write .",
    "deploy": "bash gh-deploy.sh"
  },
  "dependencies": {
    "eslint": "^7.26.0",
    "eslint-config-prettier": "^8.3.0",
    "gh-pages": "^3.1.0",
    "prettier": "^2.3.0",
    "serve": "^11.3.2"
  }
}
